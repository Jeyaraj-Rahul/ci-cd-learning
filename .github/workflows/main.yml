name: CI Pipeline with Test

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        run: python --version

      - name: Install dependencies
        run: pip install pytest

      - name: Run tests
        run: pytest